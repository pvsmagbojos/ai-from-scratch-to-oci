# Course Workspace

Each folder in this directory maps to one phase in the master roadmap.

Phase folders are stable. Create topic folders only when a subsection begins, using `NN-MM-topic-name`. Use the topic template in `templates/topic/`.

Do not duplicate the master roadmap inside phase folders. Use the roadmap in `docs/roadmap/` as the authoritative checklist.
